{
    api_key: "<%= @api_key%>",
    server_attributes: {
        serverHost: "<%= @node_fqdn %>",
        tier: "production"
	application: "jobs"
}

 // Log files to upload to Scalyr.
    logs: [
        {
            path: "/opt/jobs/logs/job.log",
            attributes: {parser: "accessLog"}
        },

    ],
    monitors: [
    ]
}
